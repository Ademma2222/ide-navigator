# OS — applies to the entire repo (Mac + Windows)
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# PyInstaller build output (regenerated by CI)
ide-navigator/server/build/
ide-navigator/server/dist/

# Platform-specific server binaries bundled into the extension
# (CI rebuilds per-target; locally run `npm run build:server` to regenerate)
ide-navigator/extension/bundled/

# Extension build artifacts
ide-navigator/extension/out/
ide-navigator/extension/node_modules/
ide-navigator/extension/*.vsix

# Local scratch files (not tracked)
ide-navigator/new.py
ide-navigator/large_test_file.py

# IDE / local
.vscode/
.claude/settings.local.json
